import { Twitter } from "@material-ui/icons";

const TwitterIcon = ({ className, fontSize }) => {
  return (
    <h4 className={className}>
      <Twitter fontSize={fontSize} />
    </h4>
  );
};

export default TwitterIcon;
